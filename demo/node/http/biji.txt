htpp  超文本传输协议
 请求与相应交互的过程
   主体对象
     服务器对象：http.cresteServer();
     客户端对象：http.request({host:'www.baidu.com'});
     请求报文对象（对于服务器来说是可读）req
     相应报文对象（对于服务器来说是可写）res

   五大特点：
     一问一答（先有请求，后有响应）
     轻便、简单快速
     无连接（不为每一个请求保持连接）
     无状态（服务器不记得客户端是谁）=> cookies
     